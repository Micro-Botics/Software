import machine


def subscribe(json_entry_file):
    """
    Función de subscribe. Subscribe al topics en el servidor,
    le llega un Json como valor de entrada que indica a que topics
    tiene que subscribirse y recibir información. Como salida debe devolver
    información del topic o topics indicados en JSON, seguir el orden del JSON proporcionado, recordad que se debe añadir el MAC a cada topic de forma: MAC_ESP/JSON_TOPICS.
    Si no hay topics devolver todos topics associados al MAC del esp.
    """

    return json_entry_file
